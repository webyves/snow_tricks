<div class="modal" tabindex="-1" role="dialog" id="confirmDeleteTrickImageModal_{{ trickImage.id }}">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">ATTENTION !!</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Confirmer la suppression de cette Image <em><strong>({{ loop.index }})</strong></em> ??</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
        <a href="{{ path('delete_trick_image', {'id': trickImage.id}) }}" class="btn btn-danger"><em class="fas fa-trash-alt"></em> Supprimer !</a>
      </div>
    </div>
  </div>
</div>